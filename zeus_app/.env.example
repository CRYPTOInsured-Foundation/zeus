# Example environment variables for zeus_app (client)
# Copy this to `.env` and fill values for your environment.

# Base HTTP API URL for the backend (use LAN IP when testing on device)
ZEUS_API_URL=http://<YOUR_LAN_IP_OR_HOST>:3000

# WebSocket / Socket.IO URL for realtime events
ZEUS_WS_URL=http://<YOUR_LAN_IP_OR_HOST>:3000

# App environment
NODE_ENV=development
